import { defineConfig } from 'vite'

export default defineConfig({
  // Replace 'portfolio' below with the EXACT name of your GitHub repo
  // e.g. if your repo URL is github.com/KunshiNS/portfolio → base: '/portfolio/'
  // e.g. if your repo URL is github.com/KunshiNS/my-portfolio → base: '/my-portfolio/'
  // Leave as '/' ONLY if you name the repo exactly: kunshins.github.io (special GitHub user site)
  base: '/2026-profile/',
})
